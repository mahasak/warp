// country > flavor > features

exports.getAvailableFeaturePackage = async (country) => {
    const countries = {
        'TH' : ['p2m_pro', 'bankslip_detection'],
        'PH' : ['p2m_lite'],
    }
}